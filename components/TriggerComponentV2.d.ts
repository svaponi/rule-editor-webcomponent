import React from 'react';
import { FormFieldTrigger } from '../model/Triggers';
export declare function TriggersComponentV2({ triggers, onChange, }: {
    triggers: FormFieldTrigger[];
    onChange: (trigger: FormFieldTrigger[]) => void;
}): React.JSX.Element;
